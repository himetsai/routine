# libSQL connection. Local dev: a plain SQLite file. Production: Turso.
DATABASE_URL=file:local.db
DATABASE_AUTH_TOKEN=

# The one passphrase that unlocks writes. Any long random string.
ROUTINE_PASSPHRASE=

# Signs the session cookie. Any long random string (e.g. `openssl rand -hex 32`).
SESSION_SECRET=
