---
import Layout from "../layouts/Layout.astro";
import App from "../app/App";
---

<Layout>
  <main class="mx-auto max-w-2xl px-4 pb-16 pt-[max(1.25rem,env(safe-area-inset-top))] sm:px-5 sm:pt-8">
    <App client:only="react" />
  </main>
</Layout>
